# Public API project used by the deployed frontend. This alias is intentionally
# public so browser requests are not redirected through Vercel SSO.
VITE_API_URL=https://server-one-gray.vercel.app/api

