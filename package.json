{
  "name": "maxwell-properties-management",
  "version": "1.0.0",
  "description": "Maxwell Properties Management - Track and manage all your rental properties in one place",
  "workspaces": ["client"],
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "cd server && npm start",
    "build": "cd client && npm run build && cd .. && node scripts/prepare-vercel-static.mjs",
    "db:migrate:sqlite": "cd server && npm run db:migrate:sqlite",
    "db:migrate:sqlite:apply": "cd server && npm run db:migrate:sqlite:apply",
    "db:verify": "cd server && npm run db:verify"
  },
  "dependencies": {
    "concurrently": "^9.0.1"
  },
  "keywords": ["property-management", "rental", "airbnb"],
  "author": "Maxwell",
  "license": "MIT"
}

